# Kinalys (Web) — QA Findings Register (consolidated)

**Repo:** kinalys-web (React/TS frontend) · **Audit:** Pass A (read-only) · **Last updated:** 2026-06-22 (session_002)
Severity: 🔴 S1 Critical · 🟠 S2 High · 🟡 S3 Medium · 🟢 S4 Low · 🔵 S5 Enhancement.
Finding IDs use the **W-** prefix (this repo). One row per finding; same ID as the per-task files (`audit/qa_findings_<task_id>.md`).
`Doc impact` = which source doc the fix should update (drives Pass B). `Fix phase` = when scheduled.

| ID | Dim | Sev | File:Line(s) | Finding | OWASP | Doc impact | Fix phase |
| --- | --- | --- | --- | --- | --- | --- | --- |
| W-001 | D2/D7 | 🟠 S2 | KnowledgeBase.tsx:30-42,271 | Unsanitized hand-rolled `renderMarkdown` (never escapes `< > &`) injected via `dangerouslySetInnerHTML` → stored XSS in KB articles, rendered to ALL roles. No DOMPurify anywhere. Only raw-HTML sink in the app. Fix: escape-then-sanitize (DOMPurify) or use react-markdown. | A03 Injection (XSS) | TRD frontend; PRD KB module | Go-live |
| W-002 | ARCH | 🟠 S2 | DemoSwitcher.tsx; App.tsx:775,458-480; client.ts:19-26 | Frontend demo scaffolding not gated behind DEMO_MODE/NODE_ENV — repo-wide grep for process.env/NODE_ENV/DEMO_MODE = ZERO hits; nothing is environment-aware. DemoSwitcher mount, X-Demo-User-Id interceptor, Fire-Breach-Alert card, seed buttons all ship/activate in any build. Frontend analog of platform F-066. The single permitted demo finding (prompt §0.5). Fix: single REACT_APP_DEMO_MODE flag gating all demo surfaces; prod tree-shakes them out + never attaches X-Demo-User-Id. | A05 Misconfiguration | TRD demo-vs-prod boundary; PRD Task 0 | Pre-prod (DEMO_MODE separation) |
| W-003 | D7/D1 | 🟠 S2 | App.tsx:116; client.ts:27-29 | `setAuthToken()` is defined + imported but NEVER called — Auth0 token obtained at App.tsx:116 is never attached to the axios Authorization header. Real (non-demo) API requests go out with no bearer → fail closed (401). App functions only via the demo X-Demo-User-Id path. Go-live auth gap (pairs with W-002). Fix: call setAuthToken(token) after getAccessTokenSilently, or attach via interceptor. | — | TRD auth contract | Go-live |
| W-004 | D7 | 🟡 S3 | App.tsx:650,654,658; ExecDashboard.tsx(10×); Scorecard/TalentGrid/COPCScorecard/PKTEngine/PredictiveAnalysis/CompetencyFramework/reportExport | Hardcoded RAG/performance-band thresholds (>=90/>=80/>=75/<70) inline in 36 places across 10 files — violates Decision 82 (bands must be tenant-configurable) + frontend one-source-per-number. Each site re-encodes boundary + color. Fix: centralize ragBand(score, bands) fed by tenant settings. | — | PRD Decision 82 | Post-Task-0 |
| W-005 | D4/D7 | 🟡 S3 | repo-wide (246× / 38 files); client.ts API layer | Pervasive `any` typing — API responses typed Record<string,any>/untyped; component state `any`. TS provides little protection on the data-crossing-API surface. Hotspots HrFlagsInbox(24)/Competency(17)/KpiTemplates(16)/Scorecard(16)/Predictive(16)/Exec(15). Fix: response interfaces in client.ts (pattern exists for some fns); enable no-explicit-any lint. | — | TRD frontend | Post-Task-0 |
| W-006 | D7 | 🟢 S4 | CourseCatalog.tsx:64; App.tsx (state-based nav) | `window.location.reload()` used to refresh data after fixLmsEmojis (full-page reload vs state re-fetch). State-based nav (no router) = no deep-linking/back-button (intentional per stack facts; recorded as limitation). `window.location.href`-for-nav anti-pattern is ABSENT. Fix: state re-fetch instead of reload. | — | TRD frontend | Post-Task-0 |
| W-007 | D7 | 🟢 S4 | App.tsx:409,418,435,439; kinalys.css:1279-1390 | Design token bypass — hardcoded hex colors (`#EF4444`, `#FFFBEB`, `#B45309`) and raw px spacing in inline styles bypass `--k-*` token system; notification badges and unread-alert rows ignore theme. Privacy modal section references undefined CSS vars (`--k-font-body`, `--k-border-faint`, `--k-red`, `--k-red-faint`) → silently fall back to browser defaults, immune to theming. Fix: replace hardcoded values with `var(--k-*)` equivalents; define missing tokens in `:root`. | — | TRD frontend | Post-Task-0 |
| W-008 | D7 | 🟡 S3 | kinalys.css:1227; App.tsx sidebar block | No mobile navigation — `.k-sidebar { display:none }` at ≤768px with no hamburger/drawer replacement. Entire 28+ item nav system disappears on phone/tablet; `activeNav` state defaults to 'home' and no other page is reachable on mobile. App is desktop-only for navigation. Fix: add hamburger toggle in `.k-topbar` on mobile, slide-in drawer, or bottom nav. | — | TRD frontend | Post-Task-0 |
| W-009 | D7 | 🟡 S3 | kinalys.css:79 (`--k-text-muted`); sidebar-label, stat-label, page-sub, stat-trend | Muted text contrast failure — `--k-text-muted: #94A3B8` on `--k-bg-surface: #FFFFFF` achieves ~2.4:1 contrast (WCAG AA requires 4.5:1 for normal text). Affects sidebar section labels, stat card labels/trends, page subtitles — all rendered as 11-12px text. `--k-text-disabled: #CBD5E1` is worse (~1.6:1). `--k-text-secondary` (#475569) passes. Fix: darken `--k-text-muted` to ≥`#64748B` (~4.6:1 on white). | A11y 1.4.3 | TRD frontend; design system | Pre-prod |
| W-010 | D7 | 🟢 S4 | kinalys.css:9; public/index.html | Google Fonts loaded via CSS `@import` (render-blocking) with no `<link rel="preconnect">` or `<link rel="preload">` in index.html. Three families (Syne, DM Sans, DM Mono) add a sequential round-trip before any text renders. `display=swap` IS included (FOIT avoided). Fix: move to `<link rel="preconnect" href="...">` + `<link rel="stylesheet">` in index.html for parallel font discovery. | — | TRD frontend | Post-Task-0 |
| W-011 | D7 | 🟡 S3 | repo-wide (zero error boundaries in src/) | No React error boundaries anywhere — zero `componentDidCatch`/`ErrorBoundary` implementations. Any component throw (null reference on `as any` response, score arithmetic failure, unexpected API shape) crashes the entire mounted view blank with no recovery path. Combined with W-005 pervasive `any`, the blast radius is high. Fix: wrap `.k-main` in App.tsx with a top-level boundary; add per-page boundaries. | — | TRD frontend | Pre-prod |
| W-012 | D7 | 🟢 S4 | App.tsx:471,473,579; KnowledgeBase.tsx:119; AICoaching.tsx:102; OKR.tsx:112; BalancedScorecard.tsx:81 | `alert()` for UX feedback (4 sites: demo-breach success/error, PIP-ack failure, seed-articles success) — blocks UI, can't be themed; plus 3 silent `console.error`-only failures (AICoaching load, OKR KR load, BSC metrics load) with no user-visible message. No toast/notification system. Fix: replace `alert()` with inline state messages; add `setError()` to the 3 silent catches; add a lightweight toast context. | — | TRD frontend | Post-Task-0 |
| W-013 | D7 | 🟢 S4 | repo-wide (HrDeparturesInbox.tsx:250-255; ExecDashboard, Scorecard, COPCReport, etc.) | No loading skeleton screens — all loading states render plain text ("Loading departures…", "Loading…" in table cells) with abrupt swap to content. No content-shaped placeholder anywhere. BriefViewerModal is the one exception with a text loading state inside a modal. Fix: reusable `<Skeleton>` component with shimmer animation for highest-traffic pages (Scorecard, ExecDashboard, TalentGrid). | — | TRD frontend | Post-Task-0 |
| W-014 | D7 | 🟢 S4 | SupportTickets.tsx:71-81; ImportUsers.tsx; OneOnOne.tsx:458; HrFlagsInbox.tsx:549 | Form validation is on-submit only — no per-field blur validation, no real-time character counter (SupportTickets requires 300 chars but only shows count on error), no required-field markers (asterisk/label). Error shown at top of form not adjacent to the offending field. `disabled` when `!isGenuineComment()` with no hint of what the predicate requires. Fix: onBlur per-field validation, live `{n}/{min}` counters, asterisk markers. | — | TRD frontend | Post-Task-0 |
| W-015 | D7 | 🟢 S4 | UserManagement.tsx; ExecDashboard.tsx; Scorecard.tsx; HrFlagsInbox.tsx; ClosedFlagsHistory.tsx; COPCScorecard.tsx; HrDeparturesInbox.tsx | No column sorting on any data table — all `<th>` elements are static; clicking a header has no effect. ExecDashboard.tsx:129 sorts by score descending in code (fixed, no UI toggle). Affects all 7 major list/table views. Fix: `sortKey`/`sortDir` state + clickable `<th>` with ▲/▼ indicator; a shared `useSortableTable` hook can be extracted. | — | TRD frontend | Post-Task-0 |
| W-016 | D7 | 🟢 S4 | UserManagement; TalentGrid; ExecDashboard; ClosedFlagsHistory; HrFlagsInbox; HrDeparturesInbox; KnowledgeBase | No pagination on any list view — all list-fetching pages render the full unbounded API result set with no page controls, "load more", or virtual scrolling. API calls pass no `limit`/`offset`. Cross-ref backend F-064 (unbounded queries). At demo scale imperceptible; would degrade for tenants with 100+ employees. Mitigation: UserManagement uses server-side filtered search (partially bounds the load). Fix: add `page`/`limit` params + `<Pagination />` controls on highest-traffic tables. | — | TRD frontend | Post-prod |
| W-017 | D7 | 🟡 S3 | App.tsx:302-303,310,314-317,323 (all ~28 nav items) | Entire navigation is keyboard-inaccessible — sidebar nav items and collapsible section headers are clickable `<div>`s with NO `role`/`tabIndex`/`onKeyDown`. Keyboard-only users cannot reach or activate any nav item; combined with state-based nav (no URLs, W-006) there is no address-bar fallback. No skip-to-content link either. In-page `<button>`s remain operable (partial mitigation). WCAG 2.1.1. Fix: convert to `<button>` or add role+tabindex+key handler; add skip link. | A11y 2.1.1 | TRD frontend; design system | Go-live |
| W-018 | D7 | 🟡 S3 | App.tsx (shell divs); kinalys.css:798; public/index.html; all 31 pages | No semantic landmarks (`.k-topbar`/`.k-sidebar`/`.k-main` are divs — zero `<main>`/`<nav>`/`<header>`), broken heading hierarchy (only ONE real `<h1>` app-wide at App.tsx:72; every page title is `div.k-page-title`; the only other h-tags are inside the KnowledgeBase XSS-sink markdown string W-001), and static `<title>React App</title>` never updated (state-based nav). `<html lang="en">` passes. Screen-reader "navigate by landmark/heading" unusable. WCAG 1.3.1/2.4.2/2.4.6. Fix: semantic shell elements, `<h1>` page titles, `document.title` on nav change. | A11y 1.3.1 | TRD frontend | Go-live |
| W-019 | D7 | 🟢 S4 | PrivacyAcknowledgementModal.tsx:80,70; MarkAsDepartedModal.tsx; BriefViewerModal.tsx; UserManagement.tsx:387 | Modals not accessible — no focus trap, no focus-on-open/restore-on-close, no Escape, background not `inert`/`aria-hidden`. Privacy modal has good ARIA (role=dialog/aria-modal/autoFocus/role=alert) but `aria-labelledby="privacy-title"` references a NON-EXISTENT id (heading has class only) → dangling label, SR announces no name. MarkAsDeparted/BriefViewer/Edit-employee overlays lack dialog semantics entirely. WCAG 2.4.3/4.1.2. Fix: shared `<Modal>` with focus trap + real `aria-labelledby` id; one-line fix the dangling id now. | A11y 4.1.2 | TRD frontend | Post-Task-0 |
| W-020 | D7 | 🟢 S4 | kinalys.css:980-985 (only `.k-input:focus`); App.tsx:324,407,425; UserManagement.tsx:360,367 | No visible keyboard focus indicator on interactive elements (only `.k-input` has a focus style; ZERO `:focus-visible` rules; nav-items/buttons rely on UA default outline, often invisible on themed dark bgs). Emoji-as-icon controls lack `aria-hidden`/labels: 🔔 bell (App.tsx:407), ✖ close (:425), ⚙️/✏️/🗙 — SR reads emoji names. StatRing SVG correctly `aria-hidden` (the right pattern, unapplied elsewhere). Color-only status mostly OK (text labels accompany). WCAG 2.4.7. Fix: global `:focus-visible` outline; `aria-hidden`/`aria-label` on emoji icons. | A11y 2.4.7 | TRD frontend; design system | Post-Task-0 |
| W-021 | D9 | 🟡 S3 | App.tsx:5-36; utils/reportExport.ts:1-3; KpiTemplates.tsx:2; COPCReport.tsx:3; SixSigmaReport.tsx:3 | No code-splitting — all 31 pages imported statically in App.tsx (no React.lazy/Suspense). xlsx@0.18.5 and jspdf@4.2.1+jspdf-autotable are in the initial bundle via: App → COPCReport/SixSigmaReport → reportExport.ts (jspdf+xlsx); App → KpiTemplates → xlsx. ~420 KB gzipped of PDF/spreadsheet libs shipped to every user including employees who only use Scorecard/Learning. lucide-react and @anthropic-ai/sdk zero-imported (tree-shaken). Fix: React.lazy the 31 pages; start with COPCReport/SixSigmaReport/KpiTemplates to drop xlsx+jspdf from initial chunk. | — | TRD frontend | Post-Task-0 |
| W-022 | D9 | 🟢 S4 | App.tsx (15+ useState vars co-located) | No re-render isolation — App holds 15+ state vars (alerts, unreadCount, showAlerts, dashboardStats, profile, activeNav, hasPip, etc.); any update (notification badge, dropdown toggle) re-renders the active page. Zero React.memo/useMemo in the codebase; useCallback in 2 files only. S4 at demo scale; accumulates at production tenant scale. Fix: extract NotificationsContext; memo page components. | — | TRD frontend | Post-Task-0 |
| W-023 | D9 | 🟡 S3 | repo-wide (no cache layer); UserManagement.tsx:89 + App.tsx:117 (duplicate getDepartments) | No client-side cache/dedup — no react-query/swr/hand-rolled cache; localStorage holds only theme + demo-id, never API data. State-based nav (W-006) remounts each page on switch → every navigation refetches cold (no stale-while-revalidate); returning to a visited page reloads fully. Slow-changing reference data (departments/designations/profile/settings) refetched on every consuming page rather than shared. Amplifies W-016 (full lists refetched each visit). Fix: adopt react-query OR move static reference data into a fetch-once context. | — | TRD frontend | Post-Task-0 |
| W-024 | D9 | 🟢 S4 | repo-wide (no AbortController); UserManagement.tsx:97 (debounce, no cancel) | No request cancellation — zero AbortController/axios-cancel usage; useEffect fetches setState without mounted-check or supersession guard. Two races: rapid nav (stale fetch lands after unmount → setState-on-unmounted waste) and debounced-search-without-cancel (slow broad search can overwrite a newer narrow one). Read-only so no corruption; intermittent stale UI over real-network latency. Fix: AbortController per fetch + cleanup abort, OR react-query (also fixes W-023). | — | TRD frontend | Post-Task-0 |
| W-025 | D6 | 🟡 S3 | client.ts:8; App.tsx:116 | Hardcoded config in source — `API_URL = 'http://localhost:3000'` (client.ts:8) and Auth0 audience `'https://api.kinalys.io'` (App.tsx:116) are string literals, not env vars. No `REACT_APP_API_URL` or `REACT_APP_AUTH0_AUDIENCE` defined. `.env` exists but only sets `REACT_APP_DEMO_MODE=true` (which is never read). Production build points at localhost — the API layer is broken outside a dev machine. Fix: `REACT_APP_API_URL` + `REACT_APP_AUTH0_AUDIENCE` env vars; `.env.production`; `.env.example`. Bundle with W-002 DEMO_MODE separation sprint. | — | TRD config; DevOps | Go-live |
| W-026 | D6 | 🟢 S4 | client.ts:3,6,35 | Mojibake encoding debt — box-drawing `─` (U+2500, UTF-8: `0xE2 0x94 0x80`) stored as Latin-1 mojibake `â"€` in three comment lines. Cosmetic/comments only; no runtime effect. Fix: replace `â"€` with `─`; add `charset = utf-8` to `.editorconfig` or `.vscode/settings.json`. | — | — | Post-Task-0 |
| W-027 | IF | 🟡 S3 | BriefViewerModal.tsx:11,122,166; HrDeparturesInbox.tsx:470; UserManagement.tsx:491 | IF-008 frontend half — Role Intelligence Brief viewer renders AI narrative (`executive_summary`/`open_threads`) verbatim with NO client-side sensitivity gate/redaction; trusts backend payload entirely. Disciplinary/PIP/manager-only content (per backend F-056) can bleed through AI prose even though the UI has no labelled "PIP" section. Component comment frames audience as "successor's manager" (:11) but realized gating is HR/exec-only (canSee departures/users) → leak NOT currently realized, but latent if audience widens before F-056. All fields escaped (IF-007 XSS PASS). Fix: backend F-056 (primary); frontend confidentiality banner; don't wire to non-HR audience until F-056 lands. | — | PRD IF-008; TRD briefs | Go-live (with F-056) |
| W-028 | IF | 🟢 S4 | App.tsx:560-597 (PIP ack); OneOnOne.tsx:109-144,283-284 (sign-off); PrivacyAcknowledgementModal | IF-001/003 — acknowledgement/sign-off flows capture a boolean but never surface an evidentiary RECEIPT. PIP-ack asserts "timestamped and cannot be undone" (App.tsx:588) with no visible timestamp, no version concept sent (`acknowledgePip(id, response)`), no echo of captured response. 1-on-1 "digital sign-off" shows only Done/Pending badges — no signer/timestamp/version. Privacy-ack captures version (best of three) but shows no post-ack receipt. Weakens IF-003 employee-file evidence story on the display side. Fix: render "Acknowledged [version] on [timestamp]" receipts; add PIP version; replace PIP-ack alert() (W-012). | — | PRD IF-001/003 | Pre-prod (Tier-1) |
| W-029 | D2/D9 | 🟡 S3 | package-lock.json:17668; KpiTemplates.tsx:2; reportExport.ts:3; ImportUsers (parse) | `xlsx@0.18.5` ships CVE-2023-30533 (prototype pollution) + CVE-2024-22363 (ReDoS), both UNPATCHABLE via npm (SheetJS left npm after 0.18.5; fixes are CDN-only 0.19.3+/0.20.2+). Exploitable parse path: `XLSX.read()` on user-uploaded spreadsheets (KpiTemplates KPI import, ImportUsers bulk import). Crafted .xlsx → Object.prototype pollution in browser session. Gated to hr_admin upload roles (mitigation). Same class as platform F-075, distinct client-side instance. Fix: SheetJS CDN tarball (0.20.3+) or migrate to exceljs; coordinate with F-075. | A06 Vulnerable Components | TRD deps | Pre-prod |
| W-030 | D2/D9 | 🟢 S4 | package.json; .env; .gitignore:16-19; package-lock.json:14161 | Build/supply-chain hygiene cluster — (1) react-scripts 5.0.1 / CRA is EOL (no patch path; transitive vulns mostly dev-time + nth-check/postcss already patched in lockfile); (2) `GENERATE_SOURCEMAP=false` NOT set → prod ships source maps exposing TS source (canSee matrix, thresholds, API shapes); (3) unused `@anthropic-ai/sdk` + `lucide-react` deps (tree-shaken but dead supply-chain surface); (4) `.env` committed (`.gitignore` ignores only `.env.local` variants) — no secrets today but risky once W-025 wires real config. Fix: CRA→Vite; GENERATE_SOURCEMAP=false; uninstall unused deps; gitignore `.env` + add `.env.example`. | A05 Misconfiguration | TRD config; DevOps | Pre-prod (Tier-1) |

> task_002 PASS confirmations: Auth0 access token is in-memory (default cache), NOT in localStorage — only theme prefs
> + demo id are stored (item 19 PASS); `@anthropic-ai/sdk` is in package.json but imported NOWHERE in src → no AI key
> in the bundle (item 68 PASS; IF-007 key-leak not realized; unused dep → task_013); AI-generated content
> (AICoaching/Recommendations/BriefViewerModal) is rendered as escaped text with no raw-HTML sink (IF-007 clipboard/XSS
> PASS); `dangerouslySetInnerHTML` occurs exactly once (W-001) and there is no innerHTML/document.write/eval anywhere.

## Running totals (kinalys-web, Phase 1)
- **Counts through task_002:** 🔴 S1: 0 · 🟠 S2: 1 (W-001) · 🟡 S3: 0 · 🟢 S4: 0 · 🔵 S5: 0 — **total findings: 1.**
- **Counts through task_003:** 🔴 S1: 0 · 🟠 S2: 2 (W-001, W-002) · 🟡 S3: 0 · 🟢 S4: 0 · 🔵 S5: 0 — **total findings: 2.**
- **Counts through task_004:** 🔴 S1: 0 · 🟠 S2: 3 (…+W-003) · 🟡 S3: 2 (W-004, W-005) · 🟢 S4: 1 (W-006) · 🔵 S5: 0 — **total findings: 6.**
- **Counts through task_005:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 4 (…+W-008, W-009) · 🟢 S4: 3 (…+W-007, W-010) · 🔵 S5: 0 — **total findings: 10.**
- **Counts through task_006:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 5 (…+W-011) · 🟢 S4: 6 (…+W-012, W-013, W-014) · 🔵 S5: 0 — **total findings: 14.**
- **Counts through task_007:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 5 · 🟢 S4: 8 (…+W-015, W-016) · 🔵 S5: 0 — **total findings: 16.**
- **Counts through task_008:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 7 (…+W-017, W-018) · 🟢 S4: 10 (…+W-019, W-020) · 🔵 S5: 0 — **total findings: 20.**
- **Counts through task_009:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 8 (…+W-021) · 🟢 S4: 11 (…+W-022) · 🔵 S5: 0 — **total findings: 22.**
- **Counts through task_010:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 9 (…+W-023) · 🟢 S4: 12 (…+W-024) · 🔵 S5: 0 — **total findings: 24.**
- **Counts through task_011:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 10 (…+W-025) · 🟢 S4: 13 (…+W-026) · 🔵 S5: 0 — **total findings: 26.**
- **Counts through task_012:** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 11 (…+W-027) · 🟢 S4: 14 (…+W-028) · 🔵 S5: 0 — **total findings: 28.**
- **Counts through task_013 (FINAL):** 🔴 S1: 0 · 🟠 S2: 3 · 🟡 S3: 12 (…+W-029) · 🟢 S4: 15 (…+W-030) · 🔵 S5: 0 — **total findings: 30.**
- **Tasks completed:** task_001..task_013 — **kinalys-web Pass A (read-only) COMPLETE.**
- **Next:** operator review → Pass B (doc-impact remediation) or kinalys-platform audit continuation. Cross-repo coordination: W-002↔F-066, W-027↔F-056, W-029↔F-075.
